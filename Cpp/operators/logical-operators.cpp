#include <iostream>

// Operadores lógicos (&&, ||, !)

int main () {
    bool And = true && false; // resultado = false

    bool Or = true || false; // resultado = true

    bool Not = !false; // resultado = true

    return 0;
}